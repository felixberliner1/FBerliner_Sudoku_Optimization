[package]
name = "cnf_builder"
version = "0.1.0"
edition = "2021"

[dependencies]
varisat = "0.2.2"
structure = {path = "../structure"}
serde = {workspace = true}

[[bench]]
name = "sud_0_clues"
harness = false

[dev-dependencies]
criterion = "0.3"